% THIS FILE HAS BEEN GENERATED BY scripts/devtasks.py AND
% SHOULD NOT BE EDITED DIRECTLY
function out = NPQ_VALUES(pt, options)
    arguments
        pt (1,1) string
        options.trace (1,1) logical = 0
        options.include_calc (1,1) logical = 0
    end
    if (pt == "COND")
        global NPQ_COND;
        out = NPQ_COND;
    elseif (pt == "MOD")
        global NPQ_MOD;
        out = NPQ_MOD;
        if options.include_calc
            out = cat(2, out, NPQ_VALUES("CALC"));
        else
            names = get_names("NPQ", "CALC");
            N_CALC = length(names);
            out = cat(2, out, zeros(1, N_CALC));
        end
    elseif (pt == "RC")
        global NPQ_RC;
        out = NPQ_RC;
    elseif (pt == "VEL")
        global NPQ_Vel;
        out = NPQ_Vel;
    else
        out = zeros(0, 0);
    end
end