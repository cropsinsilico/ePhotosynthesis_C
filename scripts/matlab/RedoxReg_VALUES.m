% THIS FILE HAS BEEN GENERATED BY scripts/devtasks.py AND
% SHOULD NOT BE EDITED DIRECTLY
function out = RedoxReg_VALUES(pt, options)
    arguments
        pt (1,1) string
        options.trace (1,1) logical = 0
        options.include_calc (1,1) logical = 0
    end
    if (pt == "COND")
        global RedoxReg_COND;
        out = RedoxReg_COND;
    elseif (pt == "MOD")
        global RedoxReg_MOD;
        out = RedoxReg_MOD;
        if options.include_calc
            out = cat(2, out, RedoxReg_VALUES("CALC"));
        else
            names = get_names("RedoxReg", "CALC");
            N_CALC = length(names);
            out = cat(2, out, zeros(1, N_CALC));
        end
    elseif (pt == "POOL")
        global RedoxReg_Pool;
        out = RedoxReg_Pool;
    elseif (pt == "RC")
        global RedoxReg_RC;
        out = RedoxReg_RC;
    elseif (pt == "VEL")
        global RedoxReg_Vel;
        out = RedoxReg_Vel;
    else
        out = zeros(0, 0);
    end
end