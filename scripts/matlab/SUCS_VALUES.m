% THIS FILE HAS BEEN GENERATED BY scripts/devtasks.py AND
% SHOULD NOT BE EDITED DIRECTLY
function out = SUCS_VALUES(pt, options)
    arguments
        pt (1,1) string
        options.trace (1,1) logical = 0
        options.include_calc (1,1) logical = 0
    end
    if (pt == "COND")
        global SUCS_COND;
        out = SUCS_COND;
    elseif (pt == "MOD")
        global SUCS_MOD;
        out = SUCS_MOD;
        if options.include_calc
            out = cat(2, out, SUCS_VALUES("CALC"));
        else
            names = get_names("SUCS", "CALC");
            N_CALC = length(names);
            out = cat(2, out, zeros(1, N_CALC));
        end
    elseif (pt == "POOL")
        global SUCS_Pool;
        out = SUCS_Pool;
    elseif (pt == "RC")
        global SUCS_RC;
        out = SUCS_RC;
    elseif (pt == "VEL")
        global SUCS_Vel;
        out = SUCS_Vel;
    else
        out = zeros(0, 0);
    end
end