% THIS FILE HAS BEEN GENERATED BY scripts/devtasks.py AND
% SHOULD NOT BE EDITED DIRECTLY
function out = PS_VALUES(pt, options)
    arguments
        pt (1,1) string
        options.trace (1,1) logical = 0
        options.include_calc (1,1) logical = 0
    end
    if (pt == "COND")
        global PS_COND;
        out = PS_COND;
    elseif (pt == "MOD")
        global PS_MOD;
        out = PS_MOD;
        if options.include_calc
            out = cat(2, out, PS_VALUES("CALC"));
        else
            names = get_names("PS", "CALC");
            N_CALC = length(names);
            out = cat(2, out, zeros(1, N_CALC));
        end
    elseif (pt == "RC")
        global PS_RC;
        out = PS_RC;
    elseif (pt == "VEL")
        global PS_Vel;
        out = PS_Vel;
    else
        out = zeros(0, 0);
    end
end