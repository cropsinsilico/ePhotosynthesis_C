% THIS FILE HAS BEEN GENERATED BY scripts/devtasks.py AND
% SHOULD NOT BE EDITED DIRECTLY
function out = XanCycle_VALUES(pt, options)
    arguments
        pt (1,1) string
        options.trace (1,1) logical = 0
        options.include_calc (1,1) logical = 0
    end
    if (pt == "COND")
        global XanCycle_COND;
        out = XanCycle_COND;
    elseif (pt == "MOD")
        global XanCycle_MOD;
        out = XanCycle_MOD;
        if options.include_calc
            out = cat(2, out, XanCycle_VALUES("CALC"));
        else
            names = get_names("XanCycle", "CALC");
            N_CALC = length(names);
            out = cat(2, out, zeros(1, N_CALC));
        end
    elseif (pt == "RC")
        global XanCycle_RC;
        out = XanCycle_RC;
    elseif (pt == "VEL")
        global XanCycle_Vel;
        out = XanCycle_Vel;
    else
        out = zeros(0, 0);
    end
end